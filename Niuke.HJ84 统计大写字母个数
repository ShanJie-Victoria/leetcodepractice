//找出给定字符串中大写字符(即'A'-'Z')的个数。
#include <iostream>
#include <string>
using namespace std;

int main() {
    string str;
    getline(cin,str);
    int cnt =0 ;
    for(int i=0;i<str.length();i++)
    {
        if(str[i] >= 'A'&& str[i]<='Z') cnt++;
    }
    cout <<cnt;
}
